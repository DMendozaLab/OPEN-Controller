<Window x:Class="Cyberbear_View.MachineConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cyberbear_View"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="MachineConnectionWindow" Height="450" Width="800"
        Closing="Window_Closing">

    <StackPanel Margin="10">
        <!--Title of Window-->
        <Border BorderThickness="10">
            <TextBlock FontWeight="Bold" TextAlignment="Center" TextDecorations="underline">Machine Connection</TextBlock>
        </Border>


        <!--Number of Machines (Not visible at moment, can be later)-->
        <StackPanel Visibility ="Hidden"  Orientation="Horizontal">
            <TextBlock>How many machines?</TextBlock>
            <xctk:IntegerUpDown x:Name="NumberOfMachines" Minimum="0" Watermark="Enter Here"/>
        </StackPanel>

        <!--Connecting Grid-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!--Connection List (First column)-->
            <StackPanel Orientation="Vertical" Grid.Column="0">
                <!--Grbl Ardunio connection-->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="10">Serial port for grbl ardunio:</TextBlock>
                    <ComboBox Margin="10" Name="GrblSerialComboBox" DropDownOpened="GrblSerialComboBox_DropDownOpened"></ComboBox>
                    <!--Baud Rate-->
                    <TextBlock Margin="10">Baud Rate:</TextBlock>
                    <ComboBox Margin="10"></ComboBox>
                    
                </StackPanel>
                <!--Lights Ardunio Connection-->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="10">Serial port for lights ardunio</TextBlock>
                    <ComboBox Margin="10" Name="LightsSerialComboBox" DropDownOpened="LightsSerialComboBox_DropDownOpened"></ComboBox>
                    <TextBlock Margin="10">Baud Rate:</TextBlock>
                    <ComboBox Margin="10"></ComboBox>
                </StackPanel>
            </StackPanel>

            <!--Connection and Disconnection Buttons (second Column)-->
            <Grid Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" x:Name="Connect_Btn" Click="Connect_Btn_Click" Margin="10" Content="Connect"
                    IsEnabled="False"></Button>
                <Button Grid.Column="1" Name="Disconnect_Btn" Click="Disconnect_Btn_Click" Margin="10" Content="Disconnect"
                        IsEnabled="False"></Button>
            </Grid>
            
        </Grid>
        
        <!--Camera Settings Combo Box-->
                <Border BorderThickness="3" Grid.Column="0">
                    <ComboBox  Name="CameraList_cb" DropDownOpened="CameraList_cb_DropDownOpened" SelectionChanged="CameraList_cb_SelectionChanged"></ComboBox>
                </Border>
        
        <!--Save Folder Location-->
        <Border BorderThickness="15">
            <DockPanel>
                <!--Save Folder Location Button-->
                <Button Name="SaveFolderBtn" DockPanel.Dock="Left" Click="SaveFolderBtn_Click">
                    Click for Folder
                </Button>

                <!--Save Folder Path Textbox-->
                <TextBox Name="SaveFolderPath" DockPanel.Dock="Right" TextAlignment="Left" VerticalContentAlignment="Center" IsEnabled="False"
                                     IsReadOnly="True">

                </TextBox>

            </DockPanel>

        </Border>
        

        <!--Start Button for machine-->
        <Border BorderThickness="10">
            <Button x:Name="StartManualCycleBtn" Click="StartManualCycleBtn_Click" IsEnabled="False">Start</Button>
        </Border>
        
        <!--Stop Button for machine-->
        <Border BorderThickness="10">
            <Button x:Name="StopManualCycleBtn" Click="StopManualCycleBtn_Click" IsEnabled="False">Stop</Button>
        </Border>

       <!-- <Border BorderThickness="10">
            <Expander VerticalContentAlignment="Stretch" ExpandDirection="Down" Header="GRBL Log" IsExpanded="True">
                <ListBox Name="ListBoxHistory" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" SelectionChanged="ListBoxHistory_SelectionChanged">
                    <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel VerticalAlignment ="Top" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                </ListBox>
                
            </Expander>
        </Border> -->
        
    </StackPanel>
</Window>